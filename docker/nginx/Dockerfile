FROM nginx:1.25-alpine

RUN apk add --no-cache openssl

# SSL certs are generated by `make ssl` on the host and bind-mounted in at
# runtime (see docker-compose.yml), so nothing to COPY here.

EXPOSE 80 443
